language: java
jdk:
  - oraclejdk8
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start  
  
before_install:
  - chmod +x gradlew

install: gradle assemble
script: gradle test